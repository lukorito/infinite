{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
    "scripts": {
        "heroku-prebuild": " cd packages/server && yarn",
        "start": "lerna run start",
        "heroku-postbuild": "cd packages/client && yarn build"
    }

}
