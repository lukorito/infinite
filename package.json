{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
    "scripts": {
        "heroku-prebuild": " cd packages/server && npm run install",
        "start": "lerna run start",
        "heroku-postbuild": "cd packages/client && npm run build"
    }

}
